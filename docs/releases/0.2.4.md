# sentinel-monitor-server 0.2.4

移除 Sentinel Monitor 产品自身的“平台管理员”页面及“系统管理”中的“管理员账号”区域。管理员名称和密码只通过 Web 右上角人物图标进入 Foundation 账户设置。

系统页不再请求平台管理员列表接口；摄像头、录像、事件、PTZ、账户设置和移动端无障碍行为保持不变。Web 字体改为首次渲染前等待 Maple 字体，避免先显示默认字体再切换。

本版本同步使用 Foundation Server 0.7.3，并重新生成服务端发布归档。
