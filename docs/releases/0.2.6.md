# Sentinel Monitor Server 0.2.6

管理 Web 升级到 Foundation Server 0.7.6。菜单栏到实例工作区、系统状态卡片到最近业务审计区块，以及各视图内部内容统一采用 16px 间距；原有 1.5rem/2rem 局部外边距不再叠加。

Chromium 与 Firefox 的摄像头、录像、事件、系统状态、账户设置、移动布局和 WCAG AA 回归通过。
