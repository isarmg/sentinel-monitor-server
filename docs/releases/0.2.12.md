# Sentinel Monitor Server 0.2.12

- 强化摄像机创建、删除、预览与录像流生命周期，清理失败和并发操作可安全收敛。
- 修正 Caddy 路由重写的 CI 精确校验，并扩充服务端与浏览器回归覆盖。
- 升级到 Foundation 0.8.2 的不可变 Rust revision 与 Web Release 包。
- 只接受 0.2.12 当前 Schema、`/api/v2` 协议、凭据 envelope 与发行树。
