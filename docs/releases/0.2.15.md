# Sentinel Monitor 0.2.15

- “新建实例”直接使用默认名称创建，登录后进入实例列表，不再弹出名称输入框。
- 实例列表加入独立删除入口，并统一展示账户名、不可变内部账户 ID 和可更换密码。
- 精简实例统计为 Host 风格的“总数 / 在线”表达，移除待配对、启用/全部实例统计。
- 新建及更换密码统一为 32 位小写英文字母或数字；Client 仍只输入授权码。实例接口按账户名一次返回全部记录并稳定排序。
- 升级至 Foundation 0.8.8。
